import { ReplaceKtMenuPlacementDirectiveDirective } from './replace-kt-menu-placement-directive.directive';

describe('ReplaceKtMenuPlacementDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceKtMenuPlacementDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
